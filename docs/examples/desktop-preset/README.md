# Minimal Desktop 3.0.0 Preset fixture

This directory contains the source entries that a conforming ZIP creator places in a `.dshpreset` archive. The `integrity.json` file hashes every other entry in this directory and does not hash itself.

The fixture intentionally has no packages, skills, secrets, task templates, or portable settings. Use Extension Dock to export a final archive after adding only reviewable, portable data.
