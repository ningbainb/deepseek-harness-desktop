# Minimal Desktop compatibility plugin fixture

This fixture is intentionally metadata-only. Copy its `dsh.compatibility` shape into a real DSH bundle, keep the normal package and test setup in that bundle, and replace all example test evidence with values from the tested release.

The fixture is not an entitlement, a publishable plugin, or a substitute for public SDK capability checks.
