# 飞书 DeepSeek Harness 需求恢复核查

核查日期：2026-09-08。

## 结论

飞书 Base `DeepSeekharness需求收集与管理` 已重新读取。主表“需求收集管理”当前有 36 条记录，空表“收集表”有 0 条。历史文档记录的 24 条需求和 16 个附件已经过期：本次多出 12 条需求，且第 35、36 条内容完全重复。

核查前状态分布为：11 条“已完成”、12 条“方案设计中”、1 条“验收中”、12 条未填写状态。逐项对照仓库后，11 条“已完成”里只有第 1、8、19 条有较强的实现和测试证据；第 4、5、10、11、12、14 条仍缺对应场景的现场或端到端验收；第 9 条遗漏了“输入框随滚轮上移”，第 13 条没有找到所述表面探测修复及回归证据，不能继续按完整交付处理。

已将 21 条审计修正写回飞书，包括 8 条原“已完成”记录、原第 24 条以及新增的 12 条记录。回读得到 Base revision 118，21 条状态和解决方案与本地审计清单逐字一致，没有错位。回写后的状态分布为：3 条“已完成”、26 条“方案设计中”、7 条“验收中”，没有未填写状态。第 36 条保留为历史记录并明确标注与第 35 条重复，没有执行删除。

本次核查没有修改 DeepSeek Harness 官方源码。仓库根目录没有 `node_modules`，因此无法检查本机实际安装的官方包内容；内核对照以 `apps/dsh-desktop/package.json` 和 `pnpm-lock.yaml` 锁定的官方 `@deepseek-ai/* 0.1.1-rc.1` 契约、插件调用点及仓库实现为准。

## 逐项核查

表中序号采用本次读取顺序，仅用于本次审计定位。

| 序号 | 核查前状态 | 需求摘要 | 核查结果 |
| --- | --- | --- | --- |
| 1 | 已完成 | 标题栏遮挡顶部标签 | 证据较强。`window-chrome.mjs` 使用 32px 原生 overlay，窗口 Chrome 单测本次通过；仍可补一次打包界面截图验收。 |
| 2 | 方案设计中 | 自定义两个置顶模型 | 首版实现存在，状态合理；当前 `/model` contribution 不能被 Host 命令装饰替换，不能声称输入框与 `/model` 已完全共用投影。 |
| 3 | 方案设计中 | 记忆功能 | 首版实现存在，含 2,000 字符、2,000 条、单轮 5 条或 2,000 字符限制及 SystemPrompt 注入；真实多主体隔离和损坏恢复尚未验收。 |
| 4 | 已完成 | SSH 终端被隐藏 | SSH 面板、入口和生命周期代码存在，但没有找到“desktop profile 重启后默认可见”的专项端到端证据，建议改为验收中。 |
| 5 | 已完成 | 执行任务反复弹终端窗口 | Desktop Runtime 使用 `windowsHide: true`；表中写的显式 `CREATE_NO_WINDOW` 没有对应代码，官方子进程专项测试因官方包未安装而未能执行，建议改为验收中。 |
| 6 | 方案设计中 | 模型供应商排序与禁用 | 与第 2 条共享实现及同一 `/model` 缺口，保持方案设计中。 |
| 7 | 方案设计中 | Electron GUI 不读取系统代理 | 现有实现主要是全局或 session 级代理；无代理、系统、PAC、手动以及 API、更新、商店、插件运行时的分层策略尚未实现。 |
| 8 | 已完成 | 文件树复制路径和添加到对话 | 证据较强。`ExplorerPanel.tsx` 同时提供两个菜单项，专项测试覆盖菜单文案和调用。 |
| 9 | 已完成 | 暗色文字、输入框滚动、按钮重叠 | 解决方案只覆盖暗色文字和侧栏按钮重叠，没有覆盖“输入框随滚轮上移”，状态应退回方案设计中。 |
| 10 | 已完成 | 思考过程弹 pwsh 窗口 | 与第 5 条重叠。Runtime 启动已使用 `windowsHide`，但没有完成官方工具子进程的当前环境验证，建议合并验收。 |
| 11 | 已完成 | 壁纸不显示或不实时更新 | Skin Center 的 profile 持久化与切换链路存在，但需求描述的是聊天区实时重绘；尚缺切换、退出重进、刷新三组现场验收，建议改为验收中。 |
| 12 | 已完成 | 空配置、本地模型和无工具模型适配 | `session-recovery`、`tools-capability`、`transcript-balance` 实现均存在；尚无本次环境中的空 profile、本地模型和 MoE 无工具矩阵验证，建议改为验收中。 |
| 13 | 已完成 | 白色外壳未透明 | 没有找到与“表面探测漏标”直接对应的修复、测试和验收记录，现有透明 CSS 不能证明该复现已消失，状态应退回方案设计中。 |
| 14 | 已完成 | 文档拖拽、余额、轮次导航、插件中心 | 五个方向都有代码：DragFileInlay、Live Stats、turn navigator、社区插件市场、转录平衡守卫；这是复合需求，且附件中还包含官方包重复副本和非托管插件问题，整体不应一次性标完成，建议拆项并保持验收中。 |
| 15 | 方案设计中 | A、B 用户记录隔离 | 请求作用域、资源授权和设备主体代码存在；需要两台设备、撤销授权和 SSE 断开实测。 |
| 16 | 方案设计中 | 安全清理可执行插件列表 | 仍是修复向导设计，未发现对应的可执行完成链路，状态合理。 |
| 17 | 方案设计中 | 分层代理 | 与第 7 条重叠但范围更清楚，尚未实现，建议合并需求和验收矩阵。 |
| 18 | 方案设计中 | 个性化提示词 | 首版实现存在；旧方案示例用 `text` 和字符串 `updatedAt`，实际实现用 `content`、数值 `updatedAt`，并包含 `enabled`、`activeProfileId` 和 session scope，文档需要按实现修订。 |
| 19 | 已完成 | 导入 Claude Code、Codex 会话 | 证据较强。两个适配器专项测试本次共 9 项通过；完整打包 UI 仍可作为发布验收项。 |
| 20 | 方案设计中 | 内网云桌面启动卡 88% | 仍缺可复现 trace、阶段超时和离线最小启动验收，状态合理。 |
| 21 | 方案设计中 | 内网更新检查卡 88% | 更新器已有超时和恢复测试，但 `--disable-updater`、DNS 失败与启动 88% 的组合场景未验证，不能视为完成。 |
| 22 | 方案设计中 | 更新后卡 88% 且重复安装文件 | 包含启动和安装目录两个问题，应拆项；离线升级及覆盖安装冒烟尚未完成。 |
| 23 | 方案设计中 | 完全格式化和修复配置入口 | 仍是恢复向导设计；清理边界、备份、回滚和会话保留需分别验收。 |
| 24 | 验收中 | 皮肤持久化和设置滚动条 | 发现明确缺口。设置窗口右侧 8px resize handle 位于 `right: 2px` 并捕获 `pointerdown`，会覆盖滚动条热区；应先修复再验收。 |
| 25 | 未填写 | 鸿蒙端切换模型及多窗口显示不一致 | 发现明确缺口。移动端只在会话加载时调用 `models(sessionId)`，自身选择时才刷新；桌面或其他窗口切换模型后没有变更事件或重新读取，导致标签陈旧。 |
| 26 | 未填写 | 主窗口移动时扩展坞跟随 | 尚未实现。扩展坞只设置 `parent: mainWindow`，没有主窗口 move/resize 监听和相对位置同步。 |
| 27 | 未填写 | 拖滚动条误触窗口边框移动 | 与第 24 条同一代码缺口，应合并修复和验收。 |
| 28 | 未填写 | VIP 充值入口 | 仓库没有 VIP、支付、订单或订阅实现；需要先定义权益、价格、支付渠道、退款及服务端授权边界。 |
| 29 | 未填写 | 建立项目后修改工作目录 | 未发现更新 workspace 路径的 API；应先核对官方 Workspace 契约，再设计新建 workspace 加迁移和回滚，避免破坏既有会话引用。 |
| 30 | 未填写 | 软件优化复合项 | 需要拆分。每会话网络开关尚无实现；宠物能力已有文档；余额及峰谷价格已有 Live Stats 基础；任务看板已有关闭入口；删除、归档、分支的说明需结合官方 UI 验证。 |
| 31 | 未填写 | 增加会话压缩 | 官方 compaction 依赖已声明，凉神和值模式 preset 已挂载 basic compaction 和 compact command；通用 desktop profile 的显式入口与状态反馈未确认。 |
| 32 | 未填写 | 技能中心名称和描述规则 | 现行规则确实要求 YAML frontmatter、`name` 匹配小写 ASCII 字母、数字和连字符、`description` 非空；新手文档没有写清，属于确定的文档缺口。 |
| 33 | 未填写 | 技能导入不显示符号链接目录 | 当前代码只枚举真实目录或 Markdown 文件，并明确拒绝 bundle 内符号链接；这是安全限制，但用户文档没有说明。若支持根符号链接，需 realpath、目录包含关系和递归预算校验。 |
| 34 | 未填写 | 大图片拖入后界面卡住 | 已有针对大图的拦截与自动压缩实现；现有测试只覆盖尺寸判断和小图不改写，没有覆盖大图解码失败、overlay 复位及真实拖放卡死，建议设为验收中。 |
| 35 | 未填写 | dsh-paperclip 与原生图片预览冲突 | 未发现该插件的专门兼容规则。应记录插件版本、宿主版本和最小复现，通过兼容检测、隔离或已知问题说明处理，不修改官方内核。 |
| 36 | 未填写 | dsh-paperclip 与原生图片预览冲突 | 与第 35 条标题和详情完全相同。保留一条作为主记录，另一条标记重复并关联主记录，不直接删除历史。 |

## 验证记录

- `node scripts/verify-feature-baseline.mjs --check` 通过：17 个插件、7 个内置项、11 个桌面界面、15 个皮肤。该门禁只证明结构保全。
- Claude Code 和 Codex 导入适配器测试共 9 项通过，使用现有 Codex Node 24.19.0，未新增运行时。
- 本次补跑 5 个 Desktop Node 测试文件，共 21 项，其中 20 项通过；唯一失败是 `windows-hidden-processes.test.mjs` 无法解析 `@deepseek-ai/dsh/package.json`，原因是根目录没有 `node_modules`，不属于功能断言失败。
- 功能基线、窗口 Chrome、终端子面板、设置窗口状态和社区市场投影均有通过证据；这些结果不能替代打包应用的实际交互验收。

## 后续执行顺序

1. 先修第 24、27 条滚动条与 resize handle 冲突，以及第 25 条模型显示同步；两项都有明确代码缺口。
2. 为第 34 条增加大图拖放端到端回归，再决定是否完成。
3. 将第 14、30 条拆成原子需求，将第 5、10 条和第 7、17 条分别合并，标记第 36 条为第 35 条的重复项。
4. 补技能 frontmatter 和符号链接限制文档，解决第 32、33 条。
5. 需要完整内核验收时，复用锁文件执行定向依赖安装和串行测试；不克隆官方源码，不做全仓构建，不下载飞书附件。

## 低占用说明

本次只拉取 36 条记录的必要文本字段和图片元数据，未下载 16 个历史附件及新增图片。没有安装全仓依赖、克隆内核或生成安装包。飞书 CLI 继续使用 `D:\FeishuCLI\lark-cli.cmd`，没有重复安装。
