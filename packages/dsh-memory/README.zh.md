# dsh-memory

[English](README.md) | 中文

面向 DeepSeek Harness 的用户隔离本地记忆。每个 principal 使用独立的私有
JSON 文件，不写入远程数据库，注入模型前会做数量和长度限制。

模型只能搜索当前轮次的直接用户文本，或创建“待确认记忆建议”；只有设置页
显式保存或用户确认后才会持久化。明显的凭据会被拒绝，错误不会回显匹配内容。
